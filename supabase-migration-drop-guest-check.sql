-- ============================================================
-- 来客チェック（卓ごと）を削除した分の後片付け（任意）
--
-- アプリ側では既に使っていないため、実行しなくても動作に影響はありません。
-- データベースを整理したい場合だけ実行してください。
--
-- ※ 実行すると元に戻せません。ただし直近30日の利用実績は0件で、
--    画面へたどり着く手段も既にないため、失われる記録はありません。
--
-- ※ 開店前チェック（daily_records / items）とは別のテーブルです。
--    そちらは毎日使われているので、消してはいけません。
--
-- 実行前に、画面上部のプロジェクト名が「gamiya」になっていることを
-- 必ず確認してください（他のプロジェクトと間違えやすいため）。
-- ============================================================

drop table if exists guest_check_sessions;
drop table if exists guest_check_items;
